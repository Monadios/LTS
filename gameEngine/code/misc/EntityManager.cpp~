#include "../misc/EntityFactory.h"

EntityManager::EntityManager()
{
}

int EntityManager::generateEntity()
{
  Entity ent = factory.generateEntity();
  entities.add(ent);
}


Entity& findById(id _id);
  std::vector<Entity&> getListOfEntities();
  id generateEntity(); 
